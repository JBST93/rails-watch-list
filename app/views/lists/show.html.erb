<h1>Movies in list:  <%= @list.name %></h1>
<%= link_to "Delete List", list_path(@list), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class:"btn btn-danger" %>

<div class="row">
  <!-- Movie List Column -->
  <div class="col-md-9">
    <%= render 'movie_card' %>
  </div>

  <!-- Description Column -->
  <div class="col-md-3">
    <%= render 'list_information' %>
  </div>
</div>



<%= render 'search_movie' %>
